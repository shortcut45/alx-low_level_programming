#include "main.h"

/**
 * int mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: 1 or 0 depending on the condition
 */

int nul(int a, int b)
{
	int multiply = a * b;

	return (multiply);
}
