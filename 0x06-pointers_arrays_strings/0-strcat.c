#include "main.h"
#include <string.h>
#include <stdio.h>

/** _strcat - concatenates two strings
 * @dest - prints a string
 * @src - prints a string
 * Return: dest
 */

char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
}
